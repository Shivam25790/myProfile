# myProfile

A simple Java project using Gradle (Java 17). Run with Gradle or the Gradle wrapper (if added):

- Build: ./gradlew build  or gradle build
- Run: ./gradlew run  or gradle run
- Test: ./gradlew test  or gradle test

This repo includes a minimal App and a basic JUnit 5 test.